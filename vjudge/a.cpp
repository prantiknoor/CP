#include <bits/stdc++.h>
#define int long long
#define endline '\n'
using namespace std;

signed main() {
  ios::sync_with_stdio(0);
  cin.tie(nullptr);

  int T;
  cin >> T;
  while (T--) {
    int a, b;
    cin >> a >> b;
    cout << a*b << endline;
  }
}